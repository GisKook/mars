1.The version of software is 0.01
2.when you start the software the OS's tray will appear the software's icon
3.both ctrl+c(system) and ctrl+alt+c(custom) are the copy hotkey.
4.ctrl+v(system) is the paste way with line break; ctrl+alt+v(custom) is the paste way without line break;
tips:When you use ctrl+alt+v the clipboard's text's line break is cleared then the ctrl+v(system) will without line break too.
PS:There are still some works to do.The major is from you need.Maybe I will add a checkbox on OS's tray.So that you can use it more comfortable.
